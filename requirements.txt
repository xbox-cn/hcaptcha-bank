# hCaptcha (Steam join 页题型池) 求解器依赖
# 实测版本: Windows 11 / Python 3.13 / 2026-09-22
camoufox[geoip]>=0.5.4
playwright>=1.60.0
torch>=2.14.0
torchvision>=0.19.0
transformers>=5.16.0
opencv-python>=4.13.0
pillow>=12.0
numpy>=2.0

# 安装后还需:
#   python -m camoufox fetch     # 下载 Camoufox 浏览器
#   首次运行自动下载 GroundingDINO-tiny / DINOv2-small / CLIP-B32 / SigLIP2-base
